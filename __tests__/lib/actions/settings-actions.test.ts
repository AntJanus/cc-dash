import { describe, it } from "vitest";

describe("saveConfig", () => {
  it.todo("writes merged config to config path");
  it.todo("creates config directory if missing");
  it.todo("validates merged config before writing");
  it.todo("returns error for invalid config values");
  it.todo("preserves existing fields when updating subset");
  it.todo("calls revalidatePath after successful save");
});
