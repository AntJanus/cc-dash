import { describe, it } from "vitest";

describe("RoadmapView", () => {
  // RBRD-05: toggle between board and list views
  it.todo("renders board view by default");
  it.todo("toggles to list view when list tab is clicked");
  it.todo("toggles back to board view when board tab is clicked");
  it.todo("passes categories and sessionRefs to board view");
  it.todo("passes categories and sessionRefs to list view");
});
