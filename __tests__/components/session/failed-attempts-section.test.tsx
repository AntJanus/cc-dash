import { describe, it } from "vitest";

describe("SESS-12: FailedAttemptsSection", () => {
  it.todo("renders list of failed attempts");
  it.todo("renders task reference for each attempt");
  it.todo("resolves task reference to task name via lookup");
  it.todo("renders empty state when no failed attempts");
});
