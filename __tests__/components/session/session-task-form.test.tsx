import { describe, it } from "vitest";

describe("SessionTaskForm", () => {
  it.todo("renders description input and dependency select");
  it.todo("calls onSubmit with description and dependency on submit");
  it.todo("does not submit when description is empty");
  it.todo("resets form after successful submit when no initialValues");
  it.todo("shows existing task IDs in dependency dropdown");
  it.todo("defaults dependency to none");
  it.todo("calls onCancel when Cancel button clicked");
  it.todo("populates fields from initialValues when editing");
});
