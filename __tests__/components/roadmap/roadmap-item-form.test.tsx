import { describe, it } from "vitest";

// Wave 0: Test stubs for RoadmapItemForm component (RCRD-01, RCRD-02)
// All stubs use it.todo() since production modules do not exist yet.

describe("RoadmapItemForm", () => {
  it.todo("renders add item form with name, description, status fields");
  it.todo("calls onSubmit with form data on submit");
  it.todo("validates name is non-empty before submit");
  it.todo("resets form after successful submit");
  it.todo("renders edit mode with pre-filled values");
  it.todo("calls onCancel when cancel button clicked");
});
