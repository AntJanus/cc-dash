import { describe, it } from "vitest";

describe("SESS-02: SessionStatusBadge", () => {
  it.todo("renders current status with correct color");
  it.todo("clicking badge opens status menu");
  it.todo("selecting a status option calls onStatusChange");
  it.todo(
    "renders all 4 status options (in-progress/paused/completed/blocked)",
  );
  it.todo("displays correct label for each status");
});
