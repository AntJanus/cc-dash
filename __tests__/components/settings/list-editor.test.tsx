import { describe, it } from "vitest";

describe("ListEditor", () => {
  it.todo("renders label and existing items");
  it.todo("adds item via input + Add button");
  it.todo("adds item via Enter key");
  it.todo("removes item via X button");
  it.todo("does not add duplicate items");
  it.todo("does not add empty/whitespace items");
  it.todo("calls onChange with updated array");
});
