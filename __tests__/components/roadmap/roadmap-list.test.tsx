import { describe, it } from "vitest";

describe("RoadmapList", () => {
  // RLST-01: list grouped by category
  it.todo("groups items by category with section headings");
  it.todo("renders items as table rows within each category");

  // RLST-02: shows name, status, started, dependencies
  it.todo("displays feature name in each row");
  it.todo("displays status badge in each row");
  it.todo("displays started date in each row");
  it.todo("displays dependency indicators in each row");

  // RLST-03: filter by category
  it.todo("shows all categories by default");
  it.todo("filters to single category when selected");
  it.todo("shows all categories when 'All' filter selected");
});
