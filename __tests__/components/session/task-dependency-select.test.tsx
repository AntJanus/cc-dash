import { describe, it } from "vitest";

describe("TaskDependencySelect", () => {
  it.todo("renders select with none option and all task options");
  it.todo("shows current dependency as selected");
  it.todo("excludes current task from options");
  it.todo("calls onChange with selected task ID");
  it.todo("calls onChange with none when cleared");
});
