import { describe, it } from "vitest";

describe("RoadmapDependencyPicker", () => {
  it.todo("renders checkbox for each available item");
  it.todo("excludes current item from options");
  it.todo("checks items that are in currentDeps");
  it.todo("calls onChange with added dep when checkbox checked");
  it.todo("calls onChange with removed dep when checkbox unchecked");
  it.todo("shows item name as label");
  it.todo("shows empty state when no other items exist");
});
