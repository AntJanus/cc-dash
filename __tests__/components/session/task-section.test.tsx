import { describe, it } from "vitest";

describe("SESS-03: TaskSection", () => {
  it.todo("renders task list in file order");
  it.todo("renders checked/unchecked checkboxes (read-only)");
  it.todo("checked tasks have strikethrough and dimmed opacity");
  it.todo("renders dependency badges with tooltip");
  it.todo("renders progress summary with count text");
  it.todo("renders progress bar");
});
