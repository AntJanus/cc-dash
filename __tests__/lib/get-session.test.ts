import { describe, it } from "vitest";

describe("getSessionBySlug", () => {
  it.todo("returns null when slug not found");
  it.todo("returns null when project has no session file");
  it.todo("returns session data for valid slug");
  it.todo("returns verification sections from unknown sections");
  it.todo("builds task name lookup map");
});
