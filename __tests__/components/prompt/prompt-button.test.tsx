import { describe, it } from "vitest";

describe("PromptButton", () => {
  it.todo("renders generate prompt button");
  it.todo("opens modal on click");
  it.todo("calls generateProjectPrompt with slug");
  it.todo("shows prompt in modal after loading");
  it.todo("stops event propagation (prevents card Link navigation)");
});
